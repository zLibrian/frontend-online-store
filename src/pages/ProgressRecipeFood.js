import React, { useState } from 'react';
import Header from '../components/Header';

export default function RecipesInProgress(meal) {
  
  return (
    <div>
      <Header title="Receitas em progresso" />
    </div>
  );
}
